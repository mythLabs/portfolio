import React from 'react';
import Layout from '../components/layout';

const Blog = () => {
    return (
       <Layout>
           This is a single blog's template
       </Layout>
    )
}

export default Blog;